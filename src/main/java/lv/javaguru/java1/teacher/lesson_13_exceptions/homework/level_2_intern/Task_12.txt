Izveidojiet BankApiImpl klasi, lai tā ieviestu BankApi saskarni:

class BankApiImpl implements BankApi {

    private List<BankClient> clients;

    public BankApiImpl(List<BankClient> clients) {
        this.clients = clients;
    }

    public Optional<BankClient> findByUid(UserCredentials credentials,
                                          String uid) throws AccessDeniedException {
          // ja akreditācijas datos nav lomas Role.CAN_SEARCH_CLIENTS
         // tad metodei vajadzētu parādīt AccessDeniedException kļūdu
         // pretējā gadījumā meklēt klientu ar norādīto uid
    }

}

Ieviesiet BankApiImpl klasi un metodi findByUid().